<!DOCTYPE html>
<html>
  <head>
    <title>MyApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript">
      I18n.defaultLocale = "<%= I18n.default_locale %>";
      I18n.locale = "<%= I18n.locale %>";
    </script>
  </head>

  <body>
  
    <header class="main">
      <%= image_tag 'ruby0.png', alt: 'Project 1' %>
      <h1><%= @page_title %></h1>
    </header>

    <section class="content">

      <nav class="side_nav">
        <ul>

          <li><%= link_to t('.home'), store_index_url %></li>
          <li><%= link_to t('.products'), products_url %></li>
          <li><%= link_to t('.carts'), carts_url %></li>
          <li><%= link_to t('.line_items'), line_items_url %></li>
          <li><%= link_to t('.login'), users_url %></li>
          <li><%= link_to t('.new_user'), new_user_path %></li>

          <!--
          <li><%= @show_msg1 %><%= @show_msg2 %>&nbsp</li>
          -->

          <% if session[:user_id] %>
            <nav class="logged_in_nav">
              <ul>
                <li><%= link_to t('.orders'), orders_url %></li>
                <li><%= link_to t('.logout'), logout_path, method: :delete %></li>
              </ul>
            </nav>
          <% end %>

          <br>
          <div id="cart" class="carts">
            <%= render_if @cart && @cart.line_items.any?, partial: 'carts/cart' %>              
          </div>
        </ul>

      </nav>

      <main class='<%= controller.controller_name %>'>
        <%= yield %>
      </main>

    </section>
  </body>
</html>